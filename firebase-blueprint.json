{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile tracking credits and basic auth data",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "User email address"
        },
        "credits": {
          "type": "integer",
          "description": "Number of remaining credits"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp when user was created"
        }
      },
      "required": ["email", "credits", "createdAt"]
    },
    "FaceHash": {
      "title": "FaceHash",
      "description": "128-dimensional biometric face descriptor used to prevent multiple registrations per face",
      "type": "object",
      "properties": {
        "faceCode": {
          "type": "array",
          "items": { "type": "number" },
          "description": "Array of 128 float descriptor coordinates"
        },
        "email": {
          "type": "string",
          "description": "Email associated with the face code"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation"
        }
      },
      "required": ["faceCode", "email", "createdAt"]
    },
    "UserChat": {
      "title": "UserChat",
      "description": "Durable collection storing chat assistant conversations and image generation history per user",
      "type": "object",
      "properties": {
        "messages": {
          "type": "array",
          "description": "List of role-typed chat objects and generated art payloads"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp when the chat was last updated"
        }
      },
      "required": ["messages", "updatedAt"]
    },
    "UserImage": {
      "title": "UserImage",
      "description": "Information about a user's generated image, including its base64 data and details",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who generated this image"
        },
        "imageUrl": {
          "type": "string",
          "description": "The base64 encoded data string or image link of the generated image"
        },
        "prompt": {
          "type": "string",
          "description": "The prompt used to generate this image"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation"
        }
      },
      "required": ["userId", "imageUrl", "prompt", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Document tracking each user's visual generation credits and details"
    },
    "/face_hashes/{docId}": {
      "schema": "FaceHash",
      "description": "Biometric face signatures for anti-spam controls"
    },
    "/user_chats/{userId}": {
      "schema": "UserChat",
      "description": "User specific conversation logs and image generation history to resume seamlessly"
    },
    "/user_images/{imageId}": {
      "schema": "UserImage",
      "description": "Durable collection storing links/data of images generated by customers for their profile gallery"
    }
  }
}
